# Azurer-terraform-iac-framework
This project builds a modular Azure Landing Zone using Terraform with environment-based structure for dev and prod. It includes modules for compute, networking, key vault, SQL, and public IP, leveraging for_each and dynamic blocks for scalability and automation.
